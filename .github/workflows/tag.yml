name: Tag

env:
  NPM_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  node-version: 18.x

on:
  push:
    tags:
      - 'v*'

jobs:
  tests:
    uses: micro-lc/middleware/.github/workflows/tests.yml@public-path
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

  lint:
    uses: micro-lc/middleware/.github/workflows/lint.yml@public-path

  build:
    uses: micro-lc/middleware/.github/workflows/build.yml@public-path

  docker-build:
    needs:
      - tests
      - lint
      - build
    uses: micro-lc/middleware/.github/workflows/docker-build.yml@public-path
    with:
      version: ${GITHUB_REF#refs/tags/}
    secrets:
      username: ${{ secrets.BOT_DOCKER_USERNAME }}
      token: ${{ secrets.BOT_DOCKER_TOKEN }}

  # npm-publish:
  #   needs:
  #     - tests
  #     - lint
  #     - build

  #   name: Release package on npm
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Use Node.js ${{ env.node-version }}
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: ${{ env.node-version }}
  #         cache: yarn

  #     - name: Install
  #       run: yarn install --immutable

  #     - name: Build
  #       run: yarn build

  #     - name: Publish
  #       run: yarn npm publish
